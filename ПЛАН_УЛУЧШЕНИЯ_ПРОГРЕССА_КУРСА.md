# План улучшения отображения прогресса по курсу

## Текущее состояние

### Что уже работает
1. ✅ Пройденные уроки помечаются иконкой [`CheckCircle2`](app/course/[courseId]/page.tsx:228)
2. ✅ Прогресс-бар отображается для купленных курсов (строки 145-154)
3. ✅ Данные о покупках хранят информацию о сроке действия ([`expiresAt`](lib/storage.ts:61))
4. ✅ [`expirationInfo`](hooks/usePurchase.ts:19) доступна через хук [`usePurchase`](hooks/usePurchase.ts:23)

### Что нужно улучшить
1. ❌ **Прогресс по модулям**: нет визуальной индикации завершенности модулей
2. ❌ **Общий прогресс-бар курса**: отображается только при `progress > 0`, должен быть всегда виден
3. ❌ **Дата окончания доступа**: не показывается пользователю после покупки
4. ❌ **Статистика по модулю**: количество пройденных уроков в модуле

## Детальный план изменений

### 1. Добавить прогресс по модулям

#### Расположение: Заголовок модуля в секции "Программа курса"

**Текущий код** (строки 203-214):
```tsx
<div className="mb-4">
  <h3 className="text-lg md:text-xl font-bold text-dark-900 mb-2">
    {module.title}
  </h3>
  <p className="text-sm text-dark-600 mb-3">
    {module.description}
  </p>
  <div className="text-sm text-dark-500">
    {module.lessons.length} {module.lessons.length === 1 ? 'урок' : 'уроков'}
  </div>
</div>
```

**Необходимые изменения**:
1. Вычислить количество пройденных уроков в модуле:
   ```tsx
   const completedLessons = module.lessons.filter(lesson => 
     progressIds.includes(lesson.id)
   ).length;
   const isModuleCompleted = completedLessons === module.lessons.length;
   const moduleProgress = Math.round((completedLessons / module.lessons.length) * 100);
   ```

2. Добавить визуальную индикацию:
   - Иконка [`CheckCircle2`](app/course/[courseId]/page.tsx:228) с зеленым цветом для завершенных модулей
   - Прогресс-бар для модулей в процессе
   - Статистика: `{completedLessons}/{module.lessons.length} уроков пройдено`

**Новая структура**:
```tsx
<div className="mb-4">
  <div className="flex items-start justify-between gap-3">
    <div className="flex-1">
      <h3 className="text-lg md:text-xl font-bold text-dark-900 mb-2 flex items-center gap-2">
        {isModuleCompleted && (
          <CheckCircle2 className="w-5 h-5 text-green-600 flex-shrink-0" />
        )}
        {module.title}
      </h3>
      <p className="text-sm text-dark-600 mb-3">
        {module.description}
      </p>
    </div>
  </div>
  
  {/* Статистика модуля */}
  <div className="flex items-center gap-4 mb-2">
    <div className="text-sm text-dark-500">
      {module.lessons.length} {module.lessons.length === 1 ? 'урок' : 'уроков'}
    </div>
    {completedLessons > 0 && (
      <div className="text-sm font-medium text-green-600">
        {completedLessons} пройдено
      </div>
    )}
  </div>
  
  {/* Прогресс-бар модуля */}
  {completedLessons > 0 && !isModuleCompleted && (
    <div className="mt-2">
      <div className="h-1.5 bg-gray-200 rounded-full overflow-hidden">
        <div 
          className="h-full bg-green-500 transition-all duration-300"
          style={{ width: `${moduleProgress}%` }}
        />
      </div>
    </div>
  )}
</div>
```

### 2. Улучшить общий прогресс-бар курса

#### Расположение: Секция "Course Info" (строки 138-168)

**Проблема**: Прогресс-бар показывается только при `progress > 0` (строка 145)

**Решение**: Показывать прогресс-бар всегда для купленных курсов

**Изменения** (строки 145-155):
```tsx
{/* Всегда показывать прогресс для купленного курса */}
<div>
  <div className="flex items-center justify-between mb-2">
    <span className="text-sm text-dark-600">
      {progress > 0 ? `Прогресс: ${progress}%` : 'Прогресс: начните обучение'}
    </span>
    <span className="text-sm text-dark-600">{completed}/{total}</span>
  </div>
  <div className="progress-bar">
    <div className="progress-fill" style={{ width: `${progress}%` }} />
  </div>
</div>
```

### 3. Отобразить срок действия доступа

#### Расположение: Секция статуса купленного курса (строки 138-168)

**Необходимая функция** для форматирования даты:
```tsx
const formatExpirationDate = (expiresAt: number | null): string => {
  if (!expiresAt) return 'Безлимитный доступ';
  
  const date = new Date(expiresAt);
  return date.toLocaleDateString('ru-RU', {
    day: 'numeric',
    month: 'long',
    year: 'numeric'
  });
};
```

**Получение данных**:
```tsx
const { expirationInfo, currentPlan } = usePurchase(courseId);
```

**Новая структура** (после строки 143):
```tsx
<div className="mb-2 flex items-center gap-2">
  <CheckCircle2 className="w-5 h-5 text-green-600" />
  <span className="text-green-700 font-semibold">У вас есть полный доступ</span>
</div>

{/* Информация о сроке доступа */}
{expirationInfo && (
  <div className="mb-3 text-sm">
    {expirationInfo.expiresAt ? (
      <div className="flex items-center gap-2">
        <Clock className="w-4 h-4 text-dark-500" />
        <span className="text-dark-600">
          Доступ до: <strong>{formatExpirationDate(expirationInfo.expiresAt)}</strong>
        </span>
        {expirationInfo.daysRemaining !== null && expirationInfo.daysRemaining <= 30 && (
          <span className={`badge ${
            expirationInfo.daysRemaining <= 7 
              ? 'bg-red-100 text-red-700' 
              : 'bg-orange-100 text-orange-700'
          }`}>
            Осталось {expirationInfo.daysRemaining} {
              expirationInfo.daysRemaining === 1 ? 'день' : 
              expirationInfo.daysRemaining < 5 ? 'дня' : 'дней'
            }
          </span>
        )}
      </div>
    ) : (
      <div className="flex items-center gap-2">
        <CheckCircle2 className="w-4 h-4 text-green-600" />
        <span className="text-green-600 font-medium">Безлимитный доступ</span>
      </div>
    )}
  </div>
)}
```

### 4. Добавить импорт недостающих компонентов

В начале файла добавить:
```tsx
import { ArrowLeft, BookOpen, Layers, Video, Clock, Lock, CheckCircle2, Play } from 'lucide-react';
```
(Clock уже используется в коде, но убедиться что импортирован)

## Итоговая структура изменений

### Файл: `app/course/[courseId]/page.tsx`

#### Изменение 1: Секция "Course Info" (строки 138-168)
- Добавить функцию `formatExpirationDate`
- Получить `expirationInfo` из хука `usePurchase`
- Добавить отображение срока доступа
- Убрать условие `progress > 0` для отображения прогресс-бара

#### Изменение 2: Заголовки модулей (строки 203-214)
- Вычислить прогресс по каждому модулю
- Добавить иконку завершения для завершенных модулей
- Добавить статистику пройденных уроков
- Добавить мини-прогресс-бар для модулей в процессе

#### Изменение 3: Список уроков (строки 218-285)
- Код уже корректно отображает [`CheckCircle2`](app/course/[courseId]/page.tsx:228) для пройденных уроков
- Никаких изменений не требуется

## Дополнительные улучшения (опционально)

### Анимации
- Плавная анимация заполнения прогресс-баров
- Анимация появления иконок завершения

### Стилизация
- Использовать цвета темы для прогресс-баров
- Добавить градиенты для визуальной привлекательности

### UX улучшения
- Добавить tooltip с детальной информацией при наведении на прогресс-бар
- Показывать поздравление при завершении модуля

## Зависимости

- ✅ [`hooks/useProgress.ts`](hooks/useProgress.ts) - предоставляет `progressIds`
- ✅ [`hooks/usePurchase.ts`](hooks/usePurchase.ts) - предоставляет `expirationInfo`
- ✅ [`lucide-react`](app/course/[courseId]/page.tsx:6) - иконки уже импортированы

## Проверка после реализации

1. **Модули**:
   - [ ] Завершенные модули помечены иконкой [`CheckCircle2`](app/course/[courseId]/page.tsx:228)
   - [ ] Частично завершенные модули показывают прогресс-бар
   - [ ] Статистика пройденных уроков отображается корректно

2. **Общий прогресс**:
   - [ ] Прогресс-бар видим всегда для купленного курса
   - [ ] Процент и счетчик `X/Y` отображаются корректно

3. **Срок доступа**:
   - [ ] Для безлимитных планов: "Безлимитный доступ"
   - [ ] Для срочных планов: дата окончания
   - [ ] При истечении менее 30 дней: предупреждающий badge
   - [ ] При истечении менее 7 дней: красный badge

4. **Адаптивность**:
   - [ ] Корректное отображение на мобильных устройствах
   - [ ] Корректное отображение на планшетах
   - [ ] Корректное отображение на десктопе
